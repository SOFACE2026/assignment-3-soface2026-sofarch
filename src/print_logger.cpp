#include <iostream>

#include "print_logger.hpp"

void PrintLogger::log(std::string message)
{
    // we just print the message to the terminal
    std::cout << message << std::endl;
    // not sure if I am supposed to implement this
    // but it is easy enough that I did
}